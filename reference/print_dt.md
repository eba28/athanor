# Display a sortable, scrollable `DataTable` table

Display a sortable, scrollable `DataTable` table

## Usage

``` r
print_dt(table, dt_width = "800px")
```

## Arguments

- table:

  The input data frame to be printed.

- dt_width:

  The width of the output table.

## Value

A DataTable table.
