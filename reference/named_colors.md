# Named color palettes for athanor plots

A list of named character vectors mapping categorical values to hex
colors, used as the `clrs_specific` argument in plotting functions.

## Usage

``` r
named_colors
```
