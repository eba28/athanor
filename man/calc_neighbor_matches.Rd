% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{calc_neighbor_matches}
\alias{calc_neighbor_matches}
\title{Calculate neighbor matching scores across metadata columns}
\usage{
calc_neighbor_matches(
  seurat_obj,
  nn_names = names(seurat_obj@neighbors),
  meta_cols = c("annotated_clusters_bcr", "annotated_clusters_binary",
    "annotated_clusters_gex_bcr", "annotated_clusters_simpler", "cdr3_aa_length",
    "clone_id_unique", "isotype_stage", "locus_light", "mu_freq_bins_binary",
    "v_call_family"),
  cdr3_length_range = 1,
  return_mean = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{nn_names}{Character vector of neighbor graph names to evaluate.
Defaults to all graphs in the object.}

\item{meta_cols}{Character vector of metadata columns to evaluate.}

\item{cdr3_length_range}{Integer range for \code{cdr3_aa_length} matching.
Neighbors within this many amino acids of the query cell are counted as
matches. Defaults to 1.}

\item{return_mean}{If TRUE, return the mean across all cells; else return
per-cell values.}

\item{verbose}{If TRUE, print progress messages.}
}
\value{
Data frame with columns: Graph, Assay, Feature, Method, Score.
If \code{return_mean = TRUE}, \code{Score} is the mean across all cells; else it
contains per-cell values with an additional \code{cell_id} column.
}
\description{
For each cell, calculates the proportion of its nearest neighbors that share
the same value for each metadata column.
}
\details{
\code{cell_id}s are saved for subsetting later if desired. Use
\code{permute_neighbor_matches()} to compute a permuted baseline.
}
