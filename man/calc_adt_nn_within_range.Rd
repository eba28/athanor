% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{calc_adt_nn_within_range}
\alias{calc_adt_nn_within_range}
\title{calculate the proportion of neighbors within an ADT expression range}
\usage{
calc_adt_nn_within_range(
  seurat_obj,
  adt_assay = "ADT",
  feature,
  base_assay,
  k = 20,
  use_k = TRUE,
  range = 0.2,
  return_counts = FALSE
)
}
\arguments{
\item{seurat_obj}{A Seurat object containing ADT data and computed neighbor
graphs.}

\item{adt_assay}{Character. Name of the assay containing ADT data. Default
is "ADT".}

\item{feature}{Character. Name of the ADT feature to evaluate (e.g., "CD27.1",
"CD38.1").}

\item{base_assay}{Character. The assay used to compute neighbors. One of
"RNA", "GEX", "BCR", or "WNN". Default is "RNA".}

\item{k}{Numeric. Number of nearest neighbors to evaluate. Must match the k
used when computing the neighbor graph. Default is 20.}

\item{range}{Numeric. The relative threshold for considering neighbors
similar. A value of 0.20 means neighbors within ±20\% of the cell's
expression are counted. Default is 0.20.}

\item{return_counts}{Logical. If TRUE, returns the count of neighbors within
range. If FALSE, returns the proportion (count/k). Default is FALSE.}
}
\value{
A named numeric vector with one value per cell in the Seurat object.
If \code{return_counts = TRUE}, returns the count of neighbors within
range. If \code{return_counts = FALSE}, returns the proportion of
neighbors within range (ranging from 0 to 1). Vector names are cell ids.
}
\description{
For each cell in a Seurat object, this function calculates how many of its
k nearest neighbors have ADT expression within a specified threshold (default
20\%) of the cell's own ADT expression for a given feature.
}
\details{
The range is symmetric around the cell's expression value. For example,
with range = 0.20:
\itemize{
\item Lower bound = cell_expr * 0.80
\item Upper bound = cell_expr * 1.20
}
}
