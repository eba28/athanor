% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general.R
\name{seurat_pipeline}
\alias{seurat_pipeline}
\title{Run Seurat's standard pipeline}
\usage{
seurat_pipeline(
  seurat_obj,
  assay = "RNA",
  pca_name = NULL,
  nfeatures_RNA,
  perc_mt,
  num_features = 2000,
  num_pcs = 50,
  num_dims = 20,
  k_param = 20,
  normalize = TRUE,
  find_var_features = TRUE,
  cluster_res = NULL,
  filter_genes,
  ensembl_version = NULL,
  cache_file = NULL,
  post_scale = NULL,
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{The Seurat object.}

\item{assay}{Name of the assay to run the pipeline on. Defaults to \code{"RNA"}.}

\item{pca_name}{Name to give the PCA reduction. If \code{NULL}, defaults to
\code{"rpca"} for the RNA assay, or \code{paste0(tolower(assay), ".pca")} for others
(e.g. \code{"rna_bcr.pca"} for \code{assay = "RNA_BCR"}).}

\item{nfeatures_RNA}{Minimum number of RNA features to retain per cell. If
omitted, cell filtering is skipped. Only applies when \code{assay = "RNA"}.}

\item{perc_mt}{Maximum percentage of mitochondrial genes to retain. If
omitted, cell filtering is skipped. Only applies when \code{assay = "RNA"}.}

\item{num_features}{Desired number of variable features.}

\item{num_pcs}{Number of principal components to compute.}

\item{num_dims}{Number of PCA dimensions to use for neighbor finding.}

\item{k_param}{Number of nearest neighbors.}

\item{normalize}{If \code{TRUE}, normalize the assay using LogNormalize before
scaling. Set to \code{FALSE} if the assay has already been normalized.}

\item{find_var_features}{If \code{TRUE}, run \code{\link[Seurat:FindVariableFeatures]{Seurat::FindVariableFeatures()}} before
scaling. Set to \code{FALSE} if variable features are already set on the assay
(e.g. when calling this after manually setting them in \code{\link[=concatenate_gex_bcr]{concatenate_gex_bcr()}}).}

\item{cluster_res}{Clustering resolution(s). If \code{NULL}, clustering is skipped.}

\item{filter_genes}{If specified, filter out genes from this category (e.g. \code{"IG"} and/or \code{"TR"}).}

\item{ensembl_version}{Ensembl version for gene annotations (e.g. \code{"GRCh38.104"}). If \code{NULL}, uses the default in \code{\link[=get_airr_genes]{get_airr_genes()}}.}

\item{cache_file}{Passed to \code{\link[=get_airr_genes]{get_airr_genes()}}. Path to a cached RDS result to use instead of querying Ensembl.}

\item{post_scale}{Optional function applied to the \code{scale.data} matrix after
\code{\link[Seurat:ScaleData]{Seurat::ScaleData()}} but before \code{\link[Seurat:RunPCA]{Seurat::RunPCA()}} (e.g. to reweight blocks of rows
relative to one another before the joint PCA in \code{\link[=concatenate_gex_bcr]{concatenate_gex_bcr()}}).
Takes and returns a features-by-cells matrix.}

\item{verbose}{Logical indicating whether or not to print messages.}
}
\value{
A processed Seurat object with PCA, neighbor graphs, optional
clusters, and UMAP. Reduction names are derived from \code{assay} and \code{pca_name}.
}
\description{
Runs the standard Seurat pipeline (normalize, scale, PCA, neighbors, UMAP) on a
Seurat object. Optionally filters cells by QC metrics first, filters IG/TR genes
from variable features, and clusters. Supports any assay (e.g. \code{"RNA"},
\code{"RNA_BCR"}), with reduction names derived automatically from the assay name.
}
\details{
Cell filtering (\code{nfeatures_RNA}, \code{perc_mt}) and ADT normalization are only
applied when \code{assay = "RNA"}. For other assays, pass \code{normalize = FALSE} if
the data has already been normalized.
}
