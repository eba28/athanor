% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gex_airr.R
\name{merge_gex_bcr}
\alias{merge_gex_bcr}
\title{Merge a GEX Seurat object with a BCR Seurat object}
\usage{
merge_gex_bcr(gex_obj, bcr_obj, transfer_reductions = TRUE, verbose = TRUE)
}
\arguments{
\item{gex_obj}{A Seurat object containing GEX (RNA) data with a \code{cell_id}
metadata column.}

\item{bcr_obj}{A Seurat object containing a BCR assay with a \code{cell_id}
metadata column (typically produced by \code{\link[=bcr_embeddings_pipeline]{bcr_embeddings_pipeline()}}).}

\item{transfer_reductions}{Whether to copy BCR reductions (\code{bpca},
\code{bcr.umap}) and graphs (\code{BCR.nn}, \code{BCR_nn}, \code{BCR_snn}) from \code{bcr_obj}
into the merged object.}

\item{verbose}{Whether or not to print a summary of the merge.}
}
\value{
A Seurat object with both RNA and BCR assays and BCR metadata columns.
}
\description{
Combines a gene expression (GEX) Seurat object and a BCR Seurat object by
matching on shared \code{cell_id} values. Handles the common case where the two
objects have different cell counts (e.g. not every GEX cell has a BCR sequence).
}
\details{
Cell matching is done via \code{cell_id} metadata, not raw Seurat barcodes.
If GEX barcodes differ from \code{cell_id} (e.g. they carry a sample prefix),
BCR cell names are renamed via \code{\link[SeuratObject:RenameCells]{SeuratObject::RenameCells()}} to match before the
assay is transferred.

Both objects are subset to their shared cells; the result is suitable for concatenation or WNN or other downstream workflows.

Due to subsetting, graphs, neighbors and reductions maybe have to be regenerated.
}
