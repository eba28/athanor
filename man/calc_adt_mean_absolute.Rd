% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{calc_adt_mean_absolute}
\alias{calc_adt_mean_absolute}
\title{Calculate mean absolute ADT distance to nearest neighbors}
\usage{
calc_adt_mean_absolute(
  seurat_obj,
  nn_names = names(seurat_obj@neighbors),
  features_adt,
  adt_assay = "ADT",
  return_mean = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{nn_names}{Character vector of neighbor graph names to evaluate.
Defaults to all graphs in the object.}

\item{features_adt}{Character vector of ADT feature names to evaluate.}

\item{adt_assay}{Name of the ADT assay.}

\item{return_mean}{If TRUE, return the mean across all cells; else return
per-cell values.}

\item{verbose}{If TRUE, print progress messages.}
}
\value{
Data frame with columns: Graph, Assay, Feature, Method, Score.
If \code{return_mean = TRUE}, \code{Score} is the mean across all cells; else it
contains per-cell values with an additional \code{cell_id} column.
}
\description{
For each cell, calculates the mean absolute difference between its ADT
expression and that of its nearest neighbors across specified features.
}
\details{
Use \code{permute_adt()} to compute a permuted baseline. For
the range-based score, see \code{calc_adt_nn_within_range()}.
}
