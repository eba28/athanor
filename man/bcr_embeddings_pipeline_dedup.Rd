% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/airr.R
\name{bcr_embeddings_pipeline_dedup}
\alias{bcr_embeddings_pipeline_dedup}
\title{Build a Seurat object from BCR embeddings, deduplicating identical embeddings first}
\usage{
bcr_embeddings_pipeline_dedup(
  embeddings,
  embedding_type,
  combined_airr = NULL,
  new_cols = NULL,
  num_pcs = 50,
  num_dims = 20,
  k_param = 20,
  verbose = TRUE
)
}
\arguments{
\item{embeddings}{Matrix of BCR embeddings (features x cells).}

\item{embedding_type}{Character label for the embedding method (stored in \code{Misc}).}

\item{combined_airr}{Optional data frame passed to \code{gex_add_airr()} to add
AIRR metadata columns. If NULL, the step is skipped.}

\item{new_cols}{Character vector of columns to add from \code{combined_airr}.
Only used when \code{combined_airr} is provided.}

\item{num_pcs}{Number of principal components to compute.}

\item{num_dims}{Number of PCA dimensions to use for neighbor finding and UMAP.}

\item{k_param}{Number of nearest neighbors.}

\item{verbose}{Logical indicating whether to print progress messages.}
}
\value{
A Seurat object as returned by \code{bcr_embeddings_pipeline()}, with all
original cells present. Cells with identical embeddings receive the same PCA
and UMAP coordinates as their first occurrence.
}
\description{
Wrapper around \code{bcr_embeddings_pipeline()} that detects cells with identical
embeddings (e.g. from clonal expansion), runs PCA, neighbor finding, and UMAP
on unique embeddings only, then copies coordinates back to all cells. This
avoids the spectral initialization hang in \code{RunUMAP()} caused by degenerate
neighbor graphs from zero-distance duplicate points.
}
